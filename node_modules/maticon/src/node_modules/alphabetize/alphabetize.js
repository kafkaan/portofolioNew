class Alphabetize {
	order(unordered) {
		var ordered = {}
		Object.keys(unordered).sort().forEach((key) => {
			ordered[key] = unordered[key];
		});
		return ordered;
	}
}

module.exports = new Alphabetize();
